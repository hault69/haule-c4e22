# Why should we use functions at all?

   #use fuctions giúp ta có cái nhìn tổng thể code một cách dễ dàng, tiết kiệm công sức gõ lại.


# How to define/declare a function?

    #Để khai báo một hàm trong Python thì chúng ta sử dụng keyword def với cú pháp như sau:
        # def ten_ham(tham_so...):
        # Trong đó:
        # ten_ham là tên của hàm mà bạn muốn đặt. Lưu ý: Tên hàm không được bắt đầu bằng số và không được chứa các ký tự đặc biệt trừ ký tự _
        # tham_so... là các tham số bạn muốn truyền vào hàm, nếu không có tham số thì để trống


# How to call/use a function?

    # Để gọi một hàm đã được khai báo rồi, thì chúng ta sử dụng cú pháp sau:
    #     ten_ham(tham_so)
        # ten_ham là tên của hàm mà bạn muốn đặt. Lưu ý: Tên hàm không được bắt đầu bằng số và không được chứa các ký tự đặc biệt trừ ký tự _
        # tham_so... là các tham số bạn muốn truyền vào hàm, nếu không có tham số thì để trống


# What is return, why and how do we use it?
# Do we have to use return in every function?
    #return là hàm có kết quả trả về
    #Trong trường hợp bạn muốn sử dụng kết quả của hàm vừa tính để thực hiện các mục đích khác. 
    # Thì bạn chỉ cần thêm keyword return trước kết quả bạn muốn trả về.
    #vd: 
        # if a == b:
        #     return True


# What are function arguments/parameters, why and how we use it?

    #tùy từng bài chúng ta sẽ khai báo tham số phù hợp với hàm cần định nghĩa.
    # dùng tham số để định nghĩa và tham chiếu.

# How to use function from a different file other than our currently working file?
    #dùng import 


